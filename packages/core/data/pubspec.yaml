name: core_data
description: Internal core data module for Fluent Starter.
version: 0.1.0
publish_to: none
resolution: workspace
environment:
  sdk: ">=3.13.0 <4.0.0"
dependencies:
  flutter:
    sdk: flutter
  core_common: ^0.1.0
  flutter_secure_storage: ^11.2.0
  shared_preferences: ^2.5.5
dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
