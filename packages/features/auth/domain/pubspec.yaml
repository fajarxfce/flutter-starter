name: auth_domain
description: Internal auth domain module for Fluent Starter.
version: 0.1.0
publish_to: none
resolution: workspace
environment:
  sdk: ">=3.13.0 <4.0.0"
dependencies:
  core_common: ^0.1.0
dev_dependencies:
  flutter_lints: ^6.0.0
  test: ^1.26.0
